# =============================================================================
# BACKEND ENVIRONMENT CONFIGURATION
# =============================================================================
# Based on comprehensive backend analysis - only includes actually used variables

# =============================================================================
# CORE APPLICATION
# =============================================================================
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001

# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL=postgresql://username:password@localhost:5432/hookly_dev

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================
# Required: Primary JWT secrets (minimum 32 characters each)
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_long_for_security_purposes
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_here_minimum_32_characters_long_different_from_above

# Required: Session secret (minimum 32 characters, must be different from JWT secrets)
SESSION_SECRET=your_session_secret_here_minimum_32_characters_long_for_session_security_purposes

# Required: Token secrets for email operations
EMAIL_VERIFICATION_SECRET=your_email_verification_secret_here_minimum_32_characters_long
PASSWORD_RESET_SECRET=your_password_reset_secret_here_minimum_32_characters_long

# Optional: JWT expiration time
JWT_EXPIRES_IN=15m

# =============================================================================
# OAUTH (GOOGLE)
# =============================================================================
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# =============================================================================
# AI SERVICES
# =============================================================================
# Primary AI provider (Gemini 2.5 Flash for cost optimization)
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# EMAIL SERVICE (SMTP)
# =============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
SMTP_FROM_EMAIL=noreply@hookly.com

# =============================================================================
# ADMIN CONFIGURATION
# =============================================================================
ADMIN_EMAILS=admin@hookly.com,admin@yourdomain.com

# =============================================================================
# PAYMENTS (LEMONSQUEEZY) - Optional for development
# =============================================================================
LEMONSQUEEZY_API_KEY=your_lemonsqueezy_api_key_here
LEMONSQUEEZY_STORE_ID=your_store_id_here
LEMONSQUEEZY_STARTER_VARIANT_ID=your_starter_plan_variant_id_here
LEMONSQUEEZY_PRO_VARIANT_ID=your_pro_plan_variant_id_here
LEMONSQUEEZY_WEBHOOK_SECRET=your_webhook_secret_here_minimum_16_characters

# =============================================================================
# MONITORING & LOGGING (OPTIONAL)
# =============================================================================
ERROR_LOG_DIR=./logs
ERROR_WEBHOOK_URL=your_error_webhook_url_here

# =============================================================================
# NOTES
# =============================================================================
# 1. REQUIRED SECRETS (minimum 32 characters each, all must be different):
#    - JWT_SECRET
#    - JWT_REFRESH_SECRET  
#    - SESSION_SECRET
#    - EMAIL_VERIFICATION_SECRET
#    - PASSWORD_RESET_SECRET
# 
# 2. DATABASE_URL is required for all environments
# 
# 3. Optional variables (app works without them but with limited functionality):
#    - GEMINI_API_KEY: AI generation uses fallback templates if missing
#    - SMTP settings: Emails logged to console if not configured
#    - LEMONSQUEEZY settings: Payment functionality disabled if missing
#    - GOOGLE OAuth: Social login disabled if missing
#
# 4. Security: Never commit actual secrets to version control
# 5. All webhook secrets should be at least 16 characters
# 6. Use strong, unique secrets for production environments